<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chatroom</title>
    <style>
        #my-video {
            display: block;
            height: 300px;
            width: 300px;
            margin: 0 auto;
        }

        video {
            width: 300px;
            height: 300px;
            object-fit: cover;
        }
    </style>
    <!-- Style sheets -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <!-- Global variables -->
    <script>
        const ROOM_ID = "<%= roomId %>";
        const USERNAME = "<%= username %>";
    </script>
    <!-- List of packages -->
    <script src="https://unpkg.com/peerjs@1.5.2/dist/peerjs.min.js" defer></script>
    <script src="/socket.io/socket.io.js" defer></script>
    <!-- Self written scripts -->
    <script src="components.js" defer></script>
    <script src="roomScript.js" defer></script>
</head>
<body>
    <nav>
        <ul style="list-style-type: none; margin: 0; padding: 0; overflow: hidden; background-color: #452c63;">
            <li style="float: left;">
              <a href="https://localhost:3000/" style="display: block; color: white; text-align: center; padding: 14px 16px; text-decoration: none;">Home</a>
            </li>
        </ul>
        </nav>
    <div id="header-container" class="col">
        <p class="h1 text-center">Start chating!</p>
        <p class="h5 text-center">Room ID: <%= roomId %></p>
    </div>
    <div class="d-flex flex-column justify-content-center">
        <div id="my-video-container"></div>
        <!-- <video id="my-video" autoplay muted></video> -->
        <div id="my-video-operation-buttons" class="d-flex flex-row justify-content-center my-2 gap-2">
          <button id="mute-unmute-button" class="btn btn-danger">Mute</button>
          <button id="play-stop-button" class="btn btn-danger">Stop</button>
          <button id="start-stop-recording-button" class="btn btn-primary">Start Recording</button>
          <button id="open-chat-button" class="btn btn-primary"><i class="fa fa-comments-o" aria-hidden="true"></i></button>
        </div>
        <div class="d-flex flex-row justify-content-center my-2 gap-2">
            <input type="file" id="audioFileInput" class="btn btn-danger" style="background-color: #a29bfe;">
          </div>
        <div class="d-flex flex-row justify-content-center my-2 gap-2">
            <audio id="audioPlayer" controls></audio></div>
      </div>
    </div>
    <div>
        <p class="h3 text-center">Participants</p>
        <div id="video-grid" class="d-flex justify-content-center gap-2"></div>
    </div>
    <div id="modal-container"></div>
</body>
</html>